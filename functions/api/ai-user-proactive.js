const json=(body,status=200,headers={})=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...headers}});
const cors=origin=>({'access-control-allow-origin':origin||'*','access-control-allow-headers':'authorization, content-type','access-control-allow-methods':'POST, OPTIONS'});
async function auth(request,env){const token=request.headers.get('authorization');const url=env.SUPABASE_URL||env.VITE_SUPABASE_URL;const key=env.SUPABASE_ANON_KEY||env.VITE_SUPABASE_ANON_KEY;if(!token||!url||!key)return null;const r=await fetch(`${url}/auth/v1/user`,{headers:{apikey:key,Authorization:token}});if(!r.ok)return null;const user=await r.json();if(!user?.id)return null;const p=await fetch(`${url}/rest/v1/profiles?select=id,role,is_online&id=eq.${encodeURIComponent(user.id)}&limit=1`,{headers:{apikey:key,Authorization:token}});const rows=p.ok?await p.json():[];return Array.isArray(rows)&&rows[0]?{user,profile:rows[0],url,key,token}:null;}
function fallback(role,metrics){const out=[];if(role==='worker'){if(!metrics.is_online)out.push({priority:'high',title:'Anda sedang offline',reason:'Status online belum aktif sehingga peluang menerima pekerjaan lebih kecil.',action:'Aktifkan status online saat siap menerima pekerjaan.'});if(Number(metrics.open_jobs||0)===0)out.push({priority:'medium',title:'Belum ada pekerjaan cocok',reason:'Belum ada lowongan terbuka yang terdeteksi dalam konteks saat ini.',action:'Perluas radius pencarian atau perbarui profil dan kategori keahlian.'});}else if(role==='employer'){if(Number(metrics.open_jobs||0)>0)out.push({priority:'high',title:'Ada lowongan belum terisi',reason:`Terdapat ${metrics.open_jobs} lowongan terbuka.`,action:'Periksa kandidat/radius dan pastikan upah serta lokasi sudah jelas.'});}if(!out.length)out.push({priority:'low',title:'Tidak ada tindakan mendesak',reason:'Tidak ditemukan sinyal yang membutuhkan intervensi sekarang.',action:'Tetap aktif dan periksa notifikasi secara berkala.'});return out.slice(0,3);}
export async function onRequest({request,env}){const headers=cors(request.headers.get('origin')||'*');if(request.method==='OPTIONS')return new Response(null,{status:204,headers});if(request.method!=='POST')return json({error:'Method Not Allowed'},405,headers);try{const a=await auth(request,env);if(!a)return json({error:'Login diperlukan.'},401,headers);const role=a.profile.role;const metrics=typeof (await request.json())?.metrics==='object'?(await request.json()).metrics:{};let recommendations=fallback(role,{...metrics,is_online:a.profile.is_online});let summary='Ada beberapa hal yang bisa Anda lakukan sekarang.';if(env.AI?.run){try{const r=await env.AI.run('@cf/meta/llama-3.1-8b-instruct',{messages:[{role:'system',content:'Anda adalah AI pendamping KerjaHarian. Berikan bantuan proaktif singkat dan operasional berdasarkan data agregat yang diberikan. Jangan mengarang fakta. Keluarkan JSON {summary,recommendations:[{priority,title,reason,action}]} dan maksimal 3 rekomendasi.'},{role:'user',content:JSON.stringify({role,metrics:{...metrics,is_online:a.profile.is_online}})}],max_tokens:500});const raw=r?.response||r?.result?.response;const m=typeof raw==='string'?raw.match(/\{[\s\S]*\}/):null;if(m){const parsed=JSON.parse(m[0]);if(typeof parsed.summary==='string'&&Array.isArray(parsed.recommendations)){summary=parsed.summary;recommendations=parsed.recommendations.slice(0,3);}}}catch{}}return json({summary,recommendations,provider:env.AI?.run?'workers-ai-or-fallback':'rule-engine'},200,headers);}catch(e){return json({error:e instanceof Error?e.message:'Gagal membuat bantuan proaktif.'},500,headers);}}
