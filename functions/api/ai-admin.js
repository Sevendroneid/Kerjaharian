import { authenticate, adminContext, clean, cors, json, model } from '../lib/ai-core.js';
function rules(ctx){const c=ctx.counts;const out=[];if(c.open>c.online_workers&&c.online_workers>=0)out.push({priority:'high',title:'Demand melebihi supply online',reason:`${c.open} open job dan ${c.online_workers} worker online.`,action:'Periksa radius dispatch, kategori, dan ketersediaan supply.'});if(c.pending_kyc>0)out.push({priority:'medium',title:'Ada KYC menunggu',reason:`${c.pending_kyc} akun berstatus pending.`,action:'Tinjau queue KYC melalui panel resmi.'});if(c.open>0)out.push({priority:'medium',title:'Ada pekerjaan menunggu matching',reason:`${c.open} job masih open.`,action:'Periksa apakah requirement sudah confirmed dan dispatch berjalan.'});if(!out.length)out.push({priority:'low',title:'Tidak ada alarm utama',reason:'Tidak ditemukan kondisi sederhana yang melewati rule dasar.',action:'Tetap pantau KPI dan lifecycle job.'});return out;}
export async function onRequest({request,env}){const headers=cors(request.headers.get('origin')||'*');if(request.method==='OPTIONS')return new Response(null,{status:204,headers});if(request.method!=='POST')return json({error:'Authentication required'},401,headers);try{const auth=await authenticate(request,env,['admin']);if(!auth)return json({error:'Akses hanya untuk admin terautentikasi.'},401,headers);const body=await request.json();const message=clean(body?.message||'Berikan ringkasan kondisi operasional KerjaHarian dan masalah yang harus saya prioritaskan.').slice(0,2000);const isAuto=body?.source==='auto';const {data:historyRows}=await auth.client.from('ai_action_logs').select('request_text,result,status,created_at').eq('actor_id',auth.user.id).eq('role','admin').order('created_at',{ascending:false}).limit(8);const history=Array.isArray(historyRows)?historyRows.map(x=>({command:String(x.request_text||''),result:x.result&&typeof x.result==='object'?x.result:{},status:String(x.status||''),at:x.created_at})).filter(x=>x.command):[];const context=await adminContext(auth);const groundedContext={...context,previous_admin_commands:history};const recommendations=rules(context);const ai=await model(message,groundedContext,env,'Anda adalah AI Admin KerjaHarian. Anda adalah otak operasional yang membantu admin seperti analyst dan problem solver. Gunakan previous_admin_commands untuk mempertahankan konteks perintah admin sebelumnya bila relevan. Baca chart/KPI dan data operasional yang diberikan, cari masalah, penyebab yang mungkin hanya jika didukung data, dan berikan solusi nyata. Anda boleh menyarankan automation dan safe action, tetapi tidak boleh melakukan critical action, mengarang angka, identitas, hukum, atau status. Untuk hukum/UU/upah eksternal, nyatakan bahwa data perlu sumber resmi terkini jika sumber belum tersedia. Jawab Bahasa Indonesia dan prioritaskan tindakan.');const summary=ai?.answer||`Monitoring selesai. ${context.counts.open} open job, ${context.counts.online_workers} worker online, ${context.counts.active} active job.`;let recorded=false;if(!isAuto){const {error}=await auth.client.from('ai_action_logs').insert({actor_id:auth.user.id,role:'admin',request_text:message,intent:'admin_command',action:'analyze_operational_request',permission_level:'information',status:'executed',evidence:{counts:context.counts,checked_at:new Date().toISOString()},recommendation:{source:'ai-admin',requires_confirmation:false},result:{summary,recommendations}});recorded=!error;}return json({summary,recommendations,context,provider:ai?.provider||'rule-engine',grounded:true,checked_at:new Date().toISOString(),command_recorded:recorded,previous_commands:history},200,headers)}catch(e){return json({error:e instanceof Error?e.message:'AI Admin gagal.'},500,headers)}}
