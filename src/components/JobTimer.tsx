import { useCallback, useEffect, useMemo, useState } from 'react';
import { AlertTriangle, Camera, CheckCircle2, Clock3, ExternalLink, MapPin, Play, TimerReset, UserCheck } from 'lucide-react';
import { supabase, type Job } from '@/lib/supabase';
import { useAuth } from '@/lib/auth';
import { calculateJobTiming } from '@/lib/jobTiming';
import { formatIDR } from '@/lib/format';
import { payCompletedJob } from '@/lib/midtrans';

interface JobTimerProps { role: 'employer' | 'worker'; lang?: 'id' | 'en'; }
type WorkflowStatus = 'assigned' | 'worker_checked_in' | 'employer_checked_in' | 'ready_to_start' | 'active' | 'overtime' | 'completed' | 'cancelled';
type WorkflowJob = Job & { workflow_status?: WorkflowStatus; worker_checked_in_at?: string | null; worker_checkin_lat?: number | null; worker_checkin_lng?: number | null; worker_checkin_photo_path?: string | null; employer_checked_in_at?: string | null; employer_checkin_lat?: number | null; employer_checkin_lng?: number | null; employer_checkin_photo_path?: string | null; employer_start_authorized_at?: string | null; employer_start_authorization_mode?: 'physical' | 'remote' | null; presence_radius_meters?: number; };
type Location = { lat: number | null; lng: number | null };

function formatDuration(minutes: number) { const safe = Number.isFinite(minutes) ? Math.max(0, Math.floor(minutes)) : 0; return `${String(Math.floor(safe / 60)).padStart(2, '0')}:${String(safe % 60).padStart(2, '0')}`; }
function isUsableJob(value: unknown): value is WorkflowJob { if (!value || typeof value !== 'object') return false; const job = value as Partial<WorkflowJob>; return typeof job.id === 'string' && typeof job.status === 'string'; }
function getJobTiming(job: WorkflowJob, now: string) { if (job.status !== 'assigned' || typeof job.started_at !== 'string' || typeof job.scheduled_end_at !== 'string') return null; const durationMinutes = Number(job.duration_minutes); if (!Number.isFinite(durationMinutes) || durationMinutes <= 0) return null; try { return calculateJobTiming({ durationMinutes, basePrice: Number(job.wage ?? 0), overtimeRatePerMinute: Number(job.overtime_rate_per_minute ?? 0), alertBeforeMinutes: 15 }, { startedAt: job.started_at, scheduledEndAt: job.scheduled_end_at, now }); } catch (cause) { console.error('KerjaHarian JobTimer timing error:', cause); return null; } }

export function JobTimer({ role, lang = 'id' }: JobTimerProps) {
  const { user } = useAuth(); const id = lang === 'id';
  const [jobs, setJobs] = useState<WorkflowJob[]>([]); const [locations, setLocations] = useState<Record<string, Location>>({}); const [workerNames, setWorkerNames] = useState<Record<string, string>>({}); const [workerPhotos, setWorkerPhotos] = useState<Record<string, string>>({}); const [now, setNow] = useState(() => new Date().toISOString()); const [loading, setLoading] = useState(true); const [busyId, setBusyId] = useState<string | null>(null); const [error, setError] = useState(''); const [photoJobId, setPhotoJobId] = useState<string | null>(null); const [payingId, setPayingId] = useState<string | null>(null);
  const copy = id ? {
    myJobs:'Pekerjaan Saya', ordersJobs:'Order & Pekerjaan', workerFlow:'Ambil order → menuju lokasi → verifikasi kehadiran → menunggu persetujuan pemberi kerja.', employerFlow:(n:number)=>`${n} order aktif. Status mitra dan timer diperbarui real-time.`, job:'Pekerjaan', location:'Lokasi belum tersedia', worker:'Mitra', workerFallback:'Mitra pekerja', payment:'Pembayaran', total:'Total', paid:'SUDAH DIBAYAR', unpaid:'BELUM DIBAYAR', openingPayment:'Membuka pembayaran…', pay:'Bayar', processingPayment:'Pembayaran sedang diproses. Status akan diperbarui otomatis setelah konfirmasi Midtrans.', navigate:'Navigasi ke Lokasi', takePhoto:'Ambil Foto Kehadiran', gpsPhoto:'GPS dan foto akan dicatat bersama waktu server. Anda harus berada dalam radius', meters:'meter.', verify:'Verifikasi Kehadiran & Foto', verified:'Kehadiran mitra terverifikasi.', waitingEmployer:'Menunggu pemberi kerja hadir di lokasi atau menyetujui mulai secara remote.', employerVerified:'Mitra sudah terverifikasi di lokasi', serverTime:'Waktu server', waitingWorker:'Menunggu mitra melakukan verifikasi kehadiran.', employerCheckin:'Saya Hadir di Lokasi & Verifikasi', remote:'Setujui & Mulai Secara Remote', bothReady:'Kedua pihak siap. Otorisasi', locationAuth:'LOKASI', start:'MULAI PEKERJAAN', summary:'Ringkasan Pembayaran', income:'Pendapatan', workerWage:'Upah Pekerja', overtime:'Lembur', status:'Status', billed:'Total Tagihan', settled:'Sudah dibayar', waitingPayment:'Menunggu pembayaran', remaining:'Sisa Waktu', dealDuration:'Durasi Deal', overtimeAmount:'Nominal Lembur', minutes:'menit', alert:(n:number)=>`Waktu kerja tersisa ${n} menit.`, finishNow:'Selesaikan sekarang = sesuai deal. Lanjutkan = waktu setelah durasi deal menjadi lembur.', finish:'Selesai', continue:'Lanjutkan', continued:'Lanjutkan sudah dipilih. Timer akan otomatis masuk lembur setelah batas deal.', dealEnded:'Durasi deal telah berakhir.', fullMinutes:'Setiap menit penuh setelah batas deal masuk ke lembur.', running:(n:number,amount:string)=>`Lembur berjalan: ${n} menit — ${amount}`, finishPayment:'Selesai & Tutup Pembayaran', workerWaitContinued:'Lembur berjalan. Menunggu pemberi kerja menekan Selesai.', workerWaitDecision:'Durasi deal selesai. Menunggu pemberi kerja memilih Selesai atau Lanjutkan.', photoAlt:'Foto verifikasi kehadiran mitra', loadingPhoto:'Memuat foto verifikasi…', overtimeLabel:'LEMBUR', activeLabel:'BERJALAN', readyLabel:'SIAP MULAI', workerArrived:'MITRA SUDAH TIBA', employerArrived:'PEMBERI KERJA SUDAH TIBA', claimed:'ORDER DIAMBIL', completed:'SELESAI', login:'Login diperlukan', image:'File harus berupa foto.', maxPhoto:'Foto maksimal 8MB.', gps:'Perangkat tidak mendukung GPS.', gpsPermission:'Izin lokasi diperlukan untuk verifikasi kehadiran.'
  } : {
    myJobs:'My Jobs', ordersJobs:'Orders & Jobs', workerFlow:'Claim job → go to location → verify attendance → wait for employer approval.', employerFlow:(n:number)=>`${n} active orders. Worker status and timer update in real time.`, job:'Job', location:'Location unavailable', worker:'Worker', workerFallback:'Worker', payment:'Payment', total:'Total', paid:'PAID', unpaid:'UNPAID', openingPayment:'Opening payment…', pay:'Pay', processingPayment:'Payment is processing. Status will update automatically after Midtrans confirmation.', navigate:'Navigate to Location', takePhoto:'Take Attendance Photo', gpsPhoto:'GPS and photo will be recorded with server time. You must be within', meters:'meters.', verify:'Verify Attendance & Photo', verified:'Worker attendance verified.', waitingEmployer:'Waiting for the employer to arrive or approve remote start.', employerVerified:'Worker verified at location', serverTime:'Server time', waitingWorker:'Waiting for the worker to verify attendance.', employerCheckin:'I Am at the Location & Verify', remote:'Approve & Start Remotely', bothReady:'Both parties are ready. Authorization', locationAuth:'LOCATION', start:'START JOB', summary:'Payment Summary', income:'Earnings', workerWage:'Worker Wage', overtime:'Overtime', status:'Status', billed:'Total Charge', settled:'Paid', waitingPayment:'Awaiting payment', remaining:'Time Remaining', dealDuration:'Agreed Duration', overtimeAmount:'Overtime Amount', minutes:'minutes', alert:(n:number)=>`${n} minutes of work time remaining.`, finishNow:'Finish now = according to the agreement. Continue = time after the agreed duration becomes overtime.', finish:'Finish', continue:'Continue', continued:'Continue was selected. The timer will enter overtime after the agreed limit.', dealEnded:'The agreed duration has ended.', fullMinutes:'Each full minute after the agreed limit is counted as overtime.', running:(n:number,amount:string)=>`Overtime running: ${n} minutes — ${amount}`, finishPayment:'Finish & Close Payment', workerWaitContinued:'Overtime is running. Waiting for the employer to press Finish.', workerWaitDecision:'The agreed duration has ended. Waiting for the employer to choose Finish or Continue.', photoAlt:'Worker attendance verification photo', loadingPhoto:'Loading verification photo…', overtimeLabel:'OVERTIME', activeLabel:'ACTIVE', readyLabel:'READY TO START', workerArrived:'WORKER ARRIVED', employerArrived:'EMPLOYER ARRIVED', claimed:'JOB CLAIMED', completed:'COMPLETED', login:'Login required', image:'File must be an image.', maxPhoto:'Photo must be 8MB or smaller.', gps:'This device does not support GPS.', gpsPermission:'Location permission is required for attendance verification.'
  };

  const load = useCallback(async () => { if (!user) { setJobs([]); setLoading(false); return; } setLoading(true); try { let query = supabase.from('jobs').select('*').order('created_at', { ascending: false }).limit(20); query = role === 'employer' ? query.eq('employer_id', user.id).in('status', ['assigned', 'completed']) : query.eq('worker_id', user.id).in('status', ['assigned', 'completed']); const { data, error: queryError } = await query; if (queryError) throw queryError; const nextJobs = (data ?? []).filter(isUsableJob); setJobs(nextJobs); const orderIds = nextJobs.map(j => j.order_id).filter((x): x is string => typeof x === 'string'); if (orderIds.length) { const { data: locs } = await supabase.from('order_locations').select('order_id,lat,lng,updated_at').in('order_id', orderIds).order('updated_at', { ascending: false }); const nextLocations: Record<string, Location> = {}; for (const loc of locs ?? []) if (!(loc.order_id in nextLocations)) nextLocations[loc.order_id] = { lat: loc.lat, lng: loc.lng }; setLocations(nextLocations); } else setLocations({}); if (role === 'employer') { const workerIds = [...new Set(nextJobs.map(j => j.worker_id).filter((x): x is string => typeof x === 'string'))]; if (workerIds.length) { const { data: profiles } = await supabase.from('profiles').select('id,full_name').in('id', workerIds); const names: Record<string, string> = {}; for (const p of profiles ?? []) names[p.id] = p.full_name || copy.workerFallback; setWorkerNames(names); } } setError(''); } catch (cause) { const message = cause instanceof Error ? cause.message : (id ? 'Gagal memuat pekerjaan.' : 'Failed to load jobs.'); console.error('KerjaHarian JobTimer load error:', cause); setError(message); } finally { setLoading(false); } }, [role, user, copy.workerFallback, id]);
  useEffect(() => { void load(); if (!user) return undefined; const channel = supabase.channel(`job-workflow-${role}-${user.id}`).on('postgres_changes', { event: '*', schema: 'public', table: 'jobs' }, () => { void load(); }).on('postgres_changes', { event: '*', schema: 'public', table: 'job_events' }, () => { void load(); }).subscribe(); return () => { void supabase.removeChannel(channel); }; }, [load, role, user]);
  useEffect(() => { if (!jobs.some(job => job.status === 'assigned' && job.started_at && job.scheduled_end_at)) return undefined; const interval = window.setInterval(() => setNow(new Date().toISOString()), 1000); return () => window.clearInterval(interval); }, [jobs]);
  const uploadAttendancePhoto = async (job: WorkflowJob, file: File) => { if (!user) throw new Error(copy.login); if (!file.type.startsWith('image/')) throw new Error(copy.image); if (file.size > 8 * 1024 * 1024) throw new Error(copy.maxPhoto); const ext = file.name.split('.').pop()?.toLowerCase() || 'jpg'; const path = `${user.id}/attendance/${job.id}-${Date.now()}.${ext}`; const { error: uploadError } = await supabase.storage.from('job-attendance').upload(path, file, { contentType: file.type, upsert: false }); if (uploadError) throw uploadError; return path; };
  const getPosition = () => new Promise<GeolocationPosition>((resolve, reject) => { if (!navigator.geolocation) { reject(new Error(copy.gps)); return; } navigator.geolocation.getCurrentPosition(resolve, () => reject(new Error(copy.gpsPermission)), { enableHighAccuracy: true, timeout: 15000, maximumAge: 0 }); });
  const workerCheckIn = async (job: WorkflowJob, file: File | null) => { setBusyId(job.id); setError(''); try { if (!file) throw new Error(id ? 'Foto kehadiran wajib diambil sebelum check-in.' : 'An attendance photo is required before check-in.'); const position = await getPosition(); const photoPath = await uploadAttendancePhoto(job, file); const { error: rpcError } = await supabase.rpc('worker_check_in', { p_job_id: job.id, p_lat: position.coords.latitude, p_lng: position.coords.longitude, p_photo_path: photoPath }); if (rpcError) throw rpcError; setPhotoJobId(null); await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : (id ? 'Gagal melakukan verifikasi kehadiran.' : 'Attendance verification failed.')); } finally { setBusyId(null); } };
  const employerCheckIn = async (job: WorkflowJob) => { setBusyId(job.id); setError(''); try { const position = await getPosition(); const { error: rpcError } = await supabase.rpc('employer_check_in', { p_job_id: job.id, p_lat: position.coords.latitude, p_lng: position.coords.longitude, p_photo_path: null }); if (rpcError) throw rpcError; await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : (id ? 'Gagal memverifikasi kehadiran pemberi kerja.' : 'Employer attendance verification failed.')); } finally { setBusyId(null); } };
  const remoteApprove = async (job: WorkflowJob) => { setBusyId(job.id); setError(''); try { const { error: rpcError } = await supabase.rpc('authorize_remote_job_start', { p_job_id: job.id }); if (rpcError) throw rpcError; await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : (id ? 'Gagal menyetujui mulai secara remote.' : 'Remote start approval failed.')); } finally { setBusyId(null); } };
  const start = async (jobId: string) => { setBusyId(jobId); setError(''); try { const { error: rpcError } = await supabase.rpc('start_job', { p_job_id: jobId }); if (rpcError) throw rpcError; await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : (id ? 'Gagal memulai pekerjaan.' : 'Failed to start job.')); } finally { setBusyId(null); } };
  const resolve = async (jobId: string, decision: 'finished' | 'continued') => { setBusyId(jobId); setError(''); try { const { error: rpcError } = await supabase.rpc('resolve_job_duration', { p_job_id: jobId, p_decision: decision }); if (rpcError) throw rpcError; await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : (id ? 'Gagal menyelesaikan pekerjaan.' : 'Failed to resolve job duration.')); } finally { setBusyId(null); } };
  const payCompleted = async (jobId: string) => { setPayingId(jobId); setError(''); try { await payCompletedJob(jobId, () => { void load(); }, () => { void load(); }); } catch (cause) { setError(cause instanceof Error ? cause.message : (id ? 'Gagal membuka pembayaran.' : 'Failed to open payment.')); } finally { setPayingId(null); } };
  const openNavigation = (job: WorkflowJob) => { const loc = job.order_id ? locations[job.order_id] : undefined; if (loc?.lat != null && loc?.lng != null) window.open(`https://www.google.com/maps/dir/?api=1&destination=${loc.lat},${loc.lng}`, '_blank', 'noopener,noreferrer'); else if (job.location) window.open(`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(job.location)}`, '_blank', 'noopener,noreferrer'); };
  const activeCount = useMemo(() => jobs.filter(j => j.status !== 'completed').length, [jobs]);
  if (!user || loading) return null; if (jobs.length === 0) return error ? <div className="mb-6 rounded-xl border border-error-200 bg-error-50 p-4 text-sm font-semibold text-error-700">{error}</div> : null;
  return <section className="mb-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <div className="flex items-center justify-between gap-3"><div><div className="flex items-center gap-2 text-primary-700"><Clock3 className="h-5 w-5"/><h2 className="font-display font-bold">{role==='worker'?copy.myJobs:copy.ordersJobs}</h2></div><p className="mt-1 text-xs text-slate-500">{role==='worker'?copy.workerFlow:copy.employerFlow(activeCount)}</p></div><TimerReset className="h-5 w-5 text-slate-300"/></div>
    {error&&<div className="mt-4 rounded-lg bg-error-50 p-3 text-xs font-semibold text-error-700">{error}</div>}
    {jobs.map(job=>{const jobTiming=getJobTiming(job,now);const overtimeAmount=jobTiming?.overtimeAmount??Number(job.overtime_amount??0);const finalAmount=Number(job.final_amount??job.employer_total??job.total??0);const workerAmount=Number(job.worker_amount??((job.wage??0)+(job.worker_overtime_amount??job.overtime_amount??0)));const alreadyContinued=job.completion_decision==='continued';const ready=job.workflow_status==='ready_to_start';const statusLabel=job.status==='completed'?copy.completed:job.workflow_status==='active'?copy.activeLabel:job.workflow_status==='overtime'?copy.overtimeLabel:ready?copy.readyLabel:job.workflow_status==='worker_checked_in'?copy.workerArrived:job.workflow_status==='employer_checked_in'?copy.employerArrived:copy.claimed;return <div key={job.id} className="mt-4 rounded-xl bg-slate-50 p-4 ring-1 ring-slate-200">
      <div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold text-slate-900">{job.title||copy.job}</p><p className="mt-1 text-xs text-slate-500">{job.location||copy.location}</p>{role==='employer'&&job.worker_id&&<p className="mt-1 text-xs font-semibold text-primary-700">{copy.worker}: {workerNames[job.worker_id]||copy.workerFallback}</p>}</div><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${job.status==='completed'?'bg-success-50 text-success-700':job.workflow_status==='overtime'?'bg-warning-100 text-warning-700':'bg-primary-50 text-primary-700'}`}>{statusLabel}</span></div>
      {role==='employer'&&job.status==='completed'&&<div className="mt-4 rounded-xl bg-white p-4 ring-1 ring-slate-200"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-bold text-slate-900">{copy.payment}</p><p className="mt-1 text-xs text-slate-500">{copy.total}: {formatIDR(finalAmount)}</p></div><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${job.payment_status==='settled'?'bg-success-50 text-success-700':'bg-warning-50 text-warning-700'}`}>{job.payment_status==='settled'?copy.paid:copy.unpaid}</span></div>{job.payment_status!=='settled'&&job.payment_status!=='refunded'&&job.payment_status!=='partial_refund'&&<button onClick={()=>void payCompleted(job.id)} disabled={payingId===job.id} className="mt-3 flex w-full items-center justify-center gap-2 rounded-lg bg-primary-600 px-3 py-2.5 text-sm font-bold text-white disabled:opacity-60">{payingId===job.id?copy.openingPayment:`${copy.pay} ${formatIDR(finalAmount)}`}</button>}{job.payment_status==='pending'&&<p className="mt-2 text-[11px] text-slate-500">{copy.processingPayment}</p>}</div>}
      {role==='worker'&&job.status!=='completed'&&!job.started_at&&<div className="mt-4 space-y-3"><button onClick={()=>openNavigation(job)} className="flex w-full items-center justify-center gap-2 rounded-lg border border-primary-200 bg-white px-3 py-2.5 text-sm font-bold text-primary-700"><ExternalLink className="h-4 w-4"/>{copy.navigate}</button>{!job.worker_checked_in_at?(photoJobId===job.id?<div className="rounded-xl bg-white p-3 ring-1 ring-slate-200"><label className="flex cursor-pointer items-center justify-center gap-2 rounded-lg border-2 border-dashed border-primary-200 px-3 py-4 text-sm font-bold text-primary-700"><Camera className="h-5 w-5"/>{copy.takePhoto}<input type="file" accept="image/*" capture="environment" className="hidden" onChange={e=>{const file=e.target.files?.[0]??null;if(file)void workerCheckIn(job,file)}}/></label><p className="mt-2 text-[11px] text-slate-500">{copy.gpsPhoto} {job.presence_radius_meters??100} {copy.meters}</p></div>:<button onClick={()=>setPhotoJobId(job.id)} disabled={busyId===job.id} className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary-600 px-3 py-2.5 text-sm font-bold text-white"><UserCheck className="h-4 w-4"/>{copy.verify}</button>):<div className="rounded-xl bg-success-50 p-3 text-xs font-semibold text-success-700 ring-1 ring-success-200"><div className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4"/>{copy.verified}</div><p className="mt-1">{copy.waitingEmployer}</p></div>}</div>}
      {role==='employer'&&job.status!=='completed'&&!job.started_at&&<div className="mt-4 space-y-3">{job.worker_checked_in_at?<div className="rounded-xl bg-success-50 p-3 ring-1 ring-success-200"><p className="text-xs font-bold text-success-800">✓ {copy.employerVerified}</p><p className="mt-1 text-[11px] text-success-700">{copy.serverTime}: {new Date(job.worker_checked_in_at).toLocaleString(id?'id-ID':'en-US')}{job.worker_checkin_lat!=null&&job.worker_checkin_lng!=null?` • GPS ${Number(job.worker_checkin_lat).toFixed(5)}, ${Number(job.worker_checkin_lng).toFixed(5)}`:''}</p>{job.worker_checkin_photo_path&&<AttendancePhoto path={job.worker_checkin_photo_path} cached={workerPhotos[job.id]} onUrl={url=>setWorkerPhotos(prev=>({...prev,[job.id]:url}))} copy={copy}/>}</div>:<div className="rounded-xl bg-warning-50 p-3 text-xs font-semibold text-warning-800 ring-1 ring-warning-200">{copy.waitingWorker}</div>}{!job.employer_checked_in_at&&<button onClick={()=>employerCheckIn(job)} disabled={busyId===job.id} className="flex w-full items-center justify-center gap-2 rounded-lg border border-primary-200 bg-white px-3 py-2.5 text-sm font-bold text-primary-700"><MapPin className="h-4 w-4"/>{copy.employerCheckin}</button>}{job.worker_checked_in_at&&!job.employer_checked_in_at&&!job.employer_start_authorized_at&&<button onClick={()=>remoteApprove(job)} disabled={busyId===job.id} className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary-600 px-3 py-2.5 text-sm font-bold text-white"><CheckCircle2 className="h-4 w-4"/>{copy.remote}</button>}{ready&&<div className="rounded-xl bg-primary-50 p-3 ring-1 ring-primary-200"><p className="text-xs font-bold text-primary-800">✓ {copy.bothReady}: {job.employer_start_authorization_mode==='remote'?'REMOTE':copy.locationAuth}</p><button onClick={()=>start(job.id)} disabled={busyId===job.id} className="mt-3 flex w-full items-center justify-center gap-2 rounded-lg bg-primary-700 px-3 py-3 text-sm font-bold text-white"><Play className="h-4 w-4"/>{copy.start}</button></div>}</div>}
      {job.status==='completed'?<div className="mt-4 rounded-xl bg-white p-4 ring-1 ring-slate-200"><p className="text-xs font-bold uppercase tracking-wide text-slate-400">{copy.summary}</p><div className="mt-3 grid gap-2 sm:grid-cols-3"><div><p className="text-[11px] text-slate-400">{role==='worker'?copy.income:copy.workerWage}</p><p className="font-bold text-slate-900">{formatIDR(workerAmount)}</p></div><div><p className="text-[11px] text-slate-400">{copy.overtime} ({Number(job.overtime_minutes??0)} {copy.minutes})</p><p className="font-bold text-warning-700">{formatIDR(Number(job.worker_overtime_amount??overtimeAmount))}</p></div><div><p className="text-[11px] text-slate-400">{copy.status==='Status'?copy.status:copy.status}</p><p className="font-bold text-primary-700">{role==='worker'?(job.payment_status==='settled'?copy.settled:copy.waitingPayment):formatIDR(finalAmount)}</p></div></div></div>:jobTiming?<><div className="mt-4 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-white p-3 ring-1 ring-slate-200"><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-400">{jobTiming.isOvertime?copy.overtime:copy.remaining}</p><p className={`mt-1 font-mono text-2xl font-extrabold ${jobTiming.isOvertime?'text-warning-600':jobTiming.isAlert?'text-warning-600':'text-slate-900'}`}>{jobTiming.isOvertime?`+${formatDuration(jobTiming.overtimeMinutes)}`:formatDuration(jobTiming.remainingMinutes)}</p></div><div className="rounded-xl bg-white p-3 ring-1 ring-slate-200"><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-400">{copy.dealDuration}</p><p className="mt-1 text-lg font-bold text-slate-900">{formatDuration(Number(job.duration_minutes??0))}</p></div><div className="rounded-xl bg-white p-3 ring-1 ring-slate-200"><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-400">{copy.overtimeAmount}</p><p className="mt-1 text-lg font-bold text-warning-700">{formatIDR(overtimeAmount)}</p></div></div>{jobTiming.isAlert&&!jobTiming.isOvertime&&!alreadyContinued&&<div className="mt-3 rounded-xl bg-warning-50 p-3 text-xs font-bold text-warning-800 ring-1 ring-warning-200"><div className="flex items-center gap-2"><AlertTriangle className="h-4 w-4 shrink-0"/>{copy.alert(jobTiming.remainingMinutes)}</div><p className="mt-1 font-medium">{copy.finishNow}</p>{role==='employer'&&<div className="mt-3 grid grid-cols-2 gap-2"><button onClick={()=>resolve(job.id,'finished')} disabled={busyId===job.id} className="flex items-center justify-center gap-1.5 rounded-lg bg-success-600 px-3 py-2.5 text-sm font-bold text-white"><CheckCircle2 className="h-4 w-4"/>{copy.finish}</button><button onClick={()=>resolve(job.id,'continued')} disabled={busyId===job.id} className="flex items-center justify-center gap-1.5 rounded-lg bg-warning-500 px-3 py-2.5 text-sm font-bold text-white"><Clock3 className="h-4 w-4"/>{copy.continue}</button></div>}</div>}{alreadyContinued&&!jobTiming.isOvertime&&<div className="mt-3 rounded-xl bg-primary-50 p-3 text-xs font-semibold text-primary-800 ring-1 ring-primary-200">{copy.continued}</div>}{jobTiming.isFinished&&role==='employer'&&!alreadyContinued&&<div className="mt-3 rounded-xl bg-error-50 p-3 ring-1 ring-error-200"><p className="text-sm font-bold text-error-800">{copy.dealEnded}</p><p className="mt-1 text-xs font-medium text-error-700">{copy.fullMinutes}</p><div className="mt-3 grid grid-cols-2 gap-2"><button onClick={()=>resolve(job.id,'finished')} disabled={busyId===job.id} className="flex items-center justify-center gap-1.5 rounded-lg bg-success-600 px-3 py-2.5 text-sm font-bold text-white"><CheckCircle2 className="h-4 w-4"/>{copy.finish}</button><button onClick={()=>resolve(job.id,'continued')} disabled={busyId===job.id} className="flex items-center justify-center gap-1.5 rounded-lg bg-warning-500 px-3 py-2.5 text-sm font-bold text-white"><Clock3 className="h-4 w-4"/>{copy.continue}</button></div></div>}{jobTiming.isOvertime&&<div className="mt-3 rounded-xl bg-warning-50 p-3 text-sm font-semibold text-warning-800 ring-1 ring-warning-200">{copy.running(jobTiming.overtimeMinutes,formatIDR(overtimeAmount))}{role==='employer'&&<button onClick={()=>resolve(job.id,'finished')} disabled={busyId===job.id} className="mt-3 flex w-full items-center justify-center gap-1.5 rounded-lg bg-success-600 px-3 py-2.5 text-sm font-bold text-white"><CheckCircle2 className="h-4 w-4"/>{copy.finishPayment}</button>}</div>}{jobTiming.isFinished&&role==='worker'&&<p className="mt-3 text-xs font-semibold text-slate-500">{alreadyContinued?copy.workerWaitContinued:copy.workerWaitDecision}</p>}</>:null}
    </div>})}
  </section>;
}

function AttendancePhoto({ path, cached, onUrl, copy }: { path: string; cached?: string; onUrl: (url: string) => void; copy: { photoAlt:string; loadingPhoto:string } }) {
  useEffect(() => { if (cached) return; let cancelled = false; void supabase.storage.from('job-attendance').createSignedUrl(path, 300).then(({ data, error }) => { if (!cancelled && !error && data?.signedUrl) onUrl(data.signedUrl); }); return () => { cancelled = true; }; }, [path, cached, onUrl]);
  return cached ? <img src={cached} alt={copy.photoAlt} className="mt-3 h-44 w-full rounded-lg object-cover ring-1 ring-success-200" /> : <div className="mt-3 flex h-32 items-center justify-center rounded-lg bg-white text-xs font-semibold text-slate-400 ring-1 ring-slate-200">{copy.loadingPhoto}</div>;
}
