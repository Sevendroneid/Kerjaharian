import { useEffect, useState } from 'react';
import { CheckCircle2, Loader2, SlidersHorizontal } from 'lucide-react';
import { supabase } from '@/lib/supabase';
import { useAuth } from '@/lib/auth';

export function WorkerMatchingProfile({ lang: initialLang }: { lang?: 'id'|'en' } = {}) {
  const { user } = useAuth();
  const [lang,setLang]=useState<'id'|'en'>(()=>initialLang??((localStorage.getItem('kerjaharian_lang') as 'id'|'en')||'id'));
  const [gender,setGender]=useState(''); const [birthYear,setBirthYear]=useState(''); const [lifting,setLifting]=useState(''); const [night,setNight]=useState(false); const [privateHome,setPrivateHome]=useState(false); const [skills,setSkills]=useState(''); const [busy,setBusy]=useState(false); const [message,setMessage]=useState('');
  const id=lang==='id';
  useEffect(()=>{if(initialLang)setLang(initialLang)},[initialLang]);
  useEffect(()=>{const sync=()=>setLang((localStorage.getItem('kerjaharian_lang') as 'id'|'en')||'id');window.addEventListener('kerjaharian-language-change',sync);return()=>window.removeEventListener('kerjaharian-language-change',sync)},[]);
  useEffect(() => { if (!user) return; void (async () => { const { data } = await supabase.from('profiles').select('gender,birth_year,lifting_capacity_kg,night_work_ok,private_home_ok,matching_skills').eq('id', user.id).maybeSingle(); if (data) { setGender(data.gender ?? ''); setBirthYear(data.birth_year ? String(data.birth_year) : ''); setLifting(data.lifting_capacity_kg != null ? String(data.lifting_capacity_kg) : ''); setNight(Boolean(data.night_work_ok)); setPrivateHome(Boolean(data.private_home_ok)); setSkills((data.matching_skills ?? []).join(', ')); } })(); }, [user]);
  const save = async () => { if (!user) return; setBusy(true); setMessage(''); const year = birthYear.trim() ? Number(birthYear) : null; const kg = lifting.trim() ? Number(lifting) : null; if (year !== null && (!Number.isInteger(year) || year < 1900 || year > new Date().getFullYear() - 18)) { setMessage(id?'Tahun lahir tidak valid.':'Invalid birth year.'); setBusy(false); return; } if (kg !== null && (!Number.isInteger(kg) || kg < 0 || kg > 100)) { setMessage(id?'Kapasitas angkat harus 0–100 kg.':'Lifting capacity must be between 0 and 100 kg.'); setBusy(false); return; } const { error } = await supabase.rpc('update_worker_matching_profile', { p_gender: gender || null, p_birth_year: year, p_lifting_capacity_kg: kg, p_night_work_ok: night, p_private_home_ok: privateHome, p_matching_skills: skills.split(',').map(s => s.trim()).filter(Boolean) }); setMessage(error ? error.message : (id?'Profil kesiapan kerja tersimpan. Data ini dipakai hanya untuk mencocokkan kebutuhan pekerjaan.':'Work readiness profile saved. This data is used only to match job requirements.')); setBusy(false); };
  if (!user) return null;
  return <section className="card mt-4 p-6"><div className="flex items-start gap-3"><SlidersHorizontal className="mt-0.5 h-5 w-5 text-primary-600"/><div><h3 className="font-display text-sm font-bold text-slate-900">{id?'Kesiapan & Kecocokan Kerja':'Work Readiness & Matching'}</h3><p className="mt-1 text-xs leading-relaxed text-slate-500">{id?'Isi yang relevan saja. Tidak wajib mengisi data yang tidak diperlukan untuk pekerjaan biasa.':'Provide only relevant information. Data that is not needed for ordinary jobs is not required.'}</p></div></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-semibold text-slate-600">{id?'Gender (opsional)':'Gender (optional)'}<select value={gender} onChange={e=>setGender(e.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 bg-white p-2.5 text-sm"><option value="">{id?'Tidak diisi':'Not provided'}</option><option value="male">{id?'Laki-laki':'Male'}</option><option value="female">{id?'Perempuan':'Female'}</option><option value="other">{id?'Lainnya':'Other'}</option><option value="prefer_not_to_say">{id?'Tidak ingin menyebutkan':'Prefer not to say'}</option></select></label><label className="text-xs font-semibold text-slate-600">{id?'Tahun lahir (opsional)':'Birth year (optional)'}<input type="number" inputMode="numeric" min="1900" max={new Date().getFullYear()-18} value={birthYear} onChange={e=>setBirthYear(e.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5 text-sm" placeholder={id?'Contoh: 1985':'Example: 1985'}/></label><label className="text-xs font-semibold text-slate-600">{id?'Kapasitas angkat (kg)':'Lifting capacity (kg)'}<input type="number" min="0" max="100" value={lifting} onChange={e=>setLifting(e.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5 text-sm" placeholder={id?'Kosongkan jika tidak perlu':'Leave blank if not needed'}/></label><label className="text-xs font-semibold text-slate-600">{id?'Keahlian':'Skills'}<input value={skills} onChange={e=>setSkills(e.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5 text-sm" placeholder={id?'Contoh: angkut, pindahan':'Example: loading, moving'}/></label></div><div className="mt-3 space-y-2 text-sm"><label className="flex items-center gap-2"><input type="checkbox" checked={night} onChange={e=>setNight(e.target.checked)}/> {id?'Bersedia kerja malam':'Available for night work'}</label><label className="flex items-center gap-2"><input type="checkbox" checked={privateHome} onChange={e=>setPrivateHome(e.target.checked)}/> {id?'Bersedia bekerja di rumah/ruang privat':'Available for work in private homes/spaces'}</label></div><button type="button" onClick={()=>void save()} disabled={busy} className="mt-4 flex w-full items-center justify-center gap-2 rounded-lg bg-primary-600 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-60">{busy?<Loader2 className="h-4 w-4 animate-spin"/>:<CheckCircle2 className="h-4 w-4"/>}{id?'Simpan Kesiapan Kerja':'Save Work Readiness'}</button>{message&&<p className="mt-3 rounded-lg bg-slate-50 p-3 text-xs font-semibold text-slate-600">{message}</p>}</section>;
}
